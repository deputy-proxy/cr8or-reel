# Editor UI primitives

The editor is currently a vanilla HTML/JS application, so shadcn/ui is applied through the same New York/zinc design tokens and interaction conventions rather than importing React-only primitives into the editor runtime. This directory is reserved for React-side shadcn primitives used by renderer tooling.
